# Project instructions

## Outcome

Describe what this application does, who uses it, and the acceptance criteria.
Resolve conflicting requirements with the user.

## Architecture

- List the existing application layers and important interfaces here.
- Preserve public APIs unless the requested task explicitly changes them.
- Keep dependencies minimal and explain any new dependency.
- Inspect nested AGENTS.md files before editing their directories.

## Verification

- Record the exact commands that build and test this project.
- Add behavioral tests for new behavior and regressions.
- Do not remove or weaken a test to conceal a failure.
- A missing compiler, browser, device, or credential is an explicit blocker.
- Report checks that could not run.

## Boundaries

- Preserve unrelated work.
- Use file tools for edits so the agent can record backups.
- Do not read credential files.
- Do not publish, deploy, push, or change external accounts unless asked.
