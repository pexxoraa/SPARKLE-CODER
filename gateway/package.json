{
  "name": "sparkle-pilot-gateway",
  "private": true,
  "devDependencies": {"wrangler": "^4.0.0"},
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "dev": "npx wrangler dev",
    "deploy": "npx wrangler deploy",
    "migrate": "npx wrangler d1 migrations apply sparkle-pilot --remote"
  }
}
